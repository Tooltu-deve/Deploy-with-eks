{
  "name": "todo-app",
  "version": "2.1.0",
  "description": "Todo List app for learning EKS deployment with CI/CD",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --verbose --forceExit --detectOpenHandles"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
